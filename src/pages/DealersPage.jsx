import React, { useEffect } from 'react';

const DealersPage = () => {
    useEffect(() => {
        window.scrollTo(0, 0);
    }, []);

    return (
        <div className="min-h-[50vh]">
            {/* Intentionally empty page as requested */}
        </div>
    );
};

export default DealersPage;
